(library
 (name runtime_compat)
 (public_name dio.runtime_compat)
 (modules runtime_compat)
 (libraries unix))

; The implementation is a single source file. The [unsafe_multidomain] alert
; suppression it contains is a no-op on the classic toolchain, so no
; toolchain-dependent selection is needed here.
