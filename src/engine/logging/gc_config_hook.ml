(* Moved to the dedicated [dio.gc_config]/[Gc_config] library: this library's name
   collides with its main module ([Logging]), so a submodule added here was not reachable
   from the lower-level domain-spawning libraries. *)
