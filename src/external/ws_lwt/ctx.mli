(* See dune: this module's implementation is generated at build time from
   ctx_lazy.ml (Conduit >= 3) or ctx_direct.ml (Conduit < 3, OxCaml). The
   compiler variant, not an environment variable, selects between them. *)

val resolve : unit -> Conduit_lwt_unix.ctx
